python-dotenv
# chroma 서버(docker-compose)가 0.5.20 이므로 클라이언트도 맞춰 고정한다.
# (1.x 클라이언트는 v2 API를 써서 0.5.x 서버와 통신 불가)
chromadb==0.5.20
# 베이스 이미지의 torch 2.3.1 과 호환되는 버전으로 고정.
# (transformers 5.x 는 torch 2.3 에서 tensor_parallel import 시 NameError 발생)
sentence-transformers==3.0.1
transformers==4.44.2
tokenizers>=0.19,<0.20
huggingface-hub>=0.24,<1.0
# datasets 5.x 는 huggingface-hub>=1 을 요구하므로 상한을 둔다.
# 하한은 4.0 — ragas>=0.4 가 datasets>=4.0.0 을 요구해서, 기존 핀(==2.21.0)으로는
# `pip install -r requirements.txt` 가 ragas 와 충돌한다(RAGAS 평가가 아예 안 깔림).
# 4.x 는 huggingface-hub<1 과 함께 동작하는 것을 확인했다(4.5.0 + hub 0.36.2).
datasets>=4,<5
pandas
numpy<2
# scipy>=1.14 는 numpy>=2 를 요구해서 numpy<2 와 충돌한다(datasets 스트리밍 로드 시 AttributeError).
scipy<1.14
torch
fastapi
uvicorn[standard]
# 데모 앱 (app.py)
streamlit
# RAGAS 평가 (evaluation/run_generation_eval.py, evaluation/run_ragas_eval.py)
# 0.4 에서 API가 바뀌었다(EvaluationDataset + 클래스형 지표). 두 스크립트 모두 0.4 기준.
ragas>=0.4,<0.5
# langchain-community 0.4 에서 chat_models.vertexai 가 제거됐는데 ragas 0.4.3 이 아직
# 이를 import 한다 -> 0.4 를 깔면 `import ragas` 자체가 ModuleNotFoundError 로 죽는다.
# RAGAS 실행 전에 반드시 이 핀으로 재설치할 것: pip install -r requirements.txt
langchain-community>=0.3,<0.4
# RAGAS judge 연결용
langchain-openai
