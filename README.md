# knowledge-rag-qa

지식 기반 RAG QA 시스템.

## 스택

| 영역 | 선택 |
|---|---|
| LLM / 임베딩 | 검토 중 |
| Vector DB | Chroma |
| UI | Streamlit |

## 실행

```bash
pip install -r requirements.txt
streamlit run app.py
```

`.env`에 API 키를 넣는다 (`.env`는 커밋하지 않음).

## 브랜치

- `main` — 통합
- `dh` — 작업용
